[package]
name = "pillovm"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = ["alloc"]
alloc = []

[dependencies]
pillow-nan = { path = "../pillow-nan" }
pillow-pack = { path = "../pillow-pack" }
